name: cron-analyze-15m
on:
  schedule:
    - cron: "*/15 * * * *" # every 15 minutes
  workflow_dispatch:

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - name: Call analyze endpoint for all pairs
        run: |
          for PAIR in BTCUSDT ETHUSDT SOLUSDT BNBUSDT XRPUSDT ADAUSDT DOGEUSDT AVAXUSDT LINKUSDT DOTUSDT; do
            echo "Analyzing $PAIR"
            curl -s -X POST "${{ secrets.APP_URL }}/api/cron/analyze?symbol=$PAIR&interval=15m" \
              -H "x-cron-secret: ${{ secrets.CRON_SECRET }}" \
              -H "Content-Type: application/json" \
              -w "\nHTTP_STATUS:%{http_code}\n"
            sleep 8
          done
          echo "Analyze all done"